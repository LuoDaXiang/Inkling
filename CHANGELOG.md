# 变更日志

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
0.x 阶段：加功能升次版本号，修 bug 升修订号。Stage 0 全部走完发 1.0.0。

---

## 未发布

**M05 服务端完成：F12 关闭，练一次四张业务表各多一行。契约落地 5/6 步，只剩客户端。**

### 新增

**存储层**（M05）

- `storage/db.ts` 连接约定：`STRICT` / WAL / `busy_timeout` / `foreign_keys` /
  `auto_vacuum`，**四条 pragma 顺序不能动**——`auto_vacuum` 排在 WAL 后面会
  静默失效（决策 0037）
- `storage/migrations.ts` 迁移：版本号存 `PRAGMA user_version`，每个迁移单独
  包事务，DDL 可回滚，跑到一半进程被杀不会留下半张表
- `storage/operations.ts` 操作流水：三条硬约束——写流水永不抛、排序靠 `id`
  不靠 `ts`、`meta` 只收白名单（决策 0038）。append-only 用触发器焊在数据库里
- `storage/records.ts` 六张表的读写，逐词明细**双写**：`words_json` 保真 +
  `phoneme_score` 扁平表供查询（决策 0039）
- 两条路由埋了 `request` / `result` / `error` 事件；`main.ts` 负责开库、迁移、
  SIGINT/SIGTERM 优雅关闭（关闭会 checkpoint 掉 `-wal`）
- `core/cost.ts` 花费换算（F13，决策 0041）：单位微元、整数运算、向上取整。
  **缓存命中 / 失败 / 未调用一律不计费**；评分按**修剪后**的时长计，
  不是用户按住录音键的时长。费率从环境变量注入，不写死
- 迁移 v3 加 `service` 列（F14，决策 0042）：`summary(a, b, 'scoring')` 才是
  「练了几次」。之前 TTS 和评分混在一个计数里，听一次范本再跟读一次会记 2

**客户端契约**（`docs/api-contract.md`）

- 826 行 75 条编号条款。改契约的顺序写死：**先改文档 → 再加测试 → 最后改代码**。
  契约文档最常见的死法不是写得不好，是写完之后代码往前跑、文档留在原地
- 完整性判据：每一条可测条款至少被一个测试引用，无法测的条款显式列出并说明为什么

**`GET /api/config`**（契约落地第 1 步）

- 新建 `src/http/contract.ts` 作为共享常量的**唯一来源**。
  `maxReferenceChars` 是 import 求值不是抄字面量——它是派生量，写死会静默错位
- `tests/contract-consistency.test.ts`：import `public/recorder.js` 的真实导出，
  与 `GET /api/config` 的响应比对。**禁止 import 服务端常量**，并有一条自检用例
  读自己的源码守着这条禁令
- 变异验证：把服务端常量改掉，两条断言都变红

**业务表接线**（契约落地第 2–5 步，F12 关闭）

- 迁移 v4：`assessment.reliable`（[C34]，趋势曲线必须过滤掉不可信的记录——
  纯白噪声的准确度是 71 分，而这件事发生时没有任何征兆）、
  `recording.trace_id`（[C74]，业务记录与流水双向可查）
- `POST /api/materials` + 列表 / 详情：分句在服务端做，`assessable` 每次算不入库
  （[C17] 它的取值将来会变）。同一段文本 POST 两次得到两个 material，刻意不幂等
- `RecordingStore`：内容哈希 + 独立目录。和 TTS 缓存的键语义**相反**——
  那边去重是目的，录音同一句录十次是十份必须各自保留的音频（[C38]）
- `POST /api/assess?sentenceId=…`：参考文本由服务端从库里读（[C25]），
  `persisted` 三态分明（[C32]），落库失败返 200 + `persistError`（[C35]）——
  评分已经成功、钱已经花了，把结果扔掉是第二次伤害
- **落盘顺序写死**（[C67]）：先写音频文件、后开事务写三张表。
  文件系统不参与 SQLite 事务，两种失败后果不对称：孤儿文件浪费磁盘，
  悬空引用是用户看到坏记录。宁可浪费磁盘

**破坏性变更**：`assess` 响应的 `seconds` / `trimmed.{start,end}` 换成
`assessedMs` / `trimmedStartMs` / `trimmedEndMs`（[C28]）。
原来一个响应里并存三种时间单位而文档只规定了其中一种，是歧义的温床。
**趁只有一个客户端时做。**

### 修复

**音频缓存会被永久写坏**（F15，决策 0040）

`FileAudioStore.put()` 原先直接 `writeFile` 到目标路径。三个各自被归档为
低优先级的缺口叠起来是一个永久损坏：F4 无去重 → 并发写同一路径 → 交错
产生坏 WAV → `get()` 只 stat 不校验 → 当成命中 → F8 无淘汰 → 永远不会被替换。

改成写临时文件 + `rename()`（同目录 rename 在 POSIX 上原子）。验证方式是
把修复临时撤回，三条并发用例立刻变红。

### 已知未完成

- 四张业务表**零数据**，没有任何生产调用方（F12）。卡在 API 缺口而非实现缺口：
  客户端不发 `sentenceId`，评分结果找不到该挂在哪一行

M05 的三条验收里，「失败几次」和「花了多少」已经答得上，「练了几次」也对了。

### 规模

| | v0.2.0 | 未发布 |
| --- | --- | --- |
| 离线用例 | 743 | **1024** |
| 测试文件 | 18 | 32 |
| 决策记录 | 34 | 43 |
| 运行时依赖 | 0 | **0** |

---

## v0.2.0 — 2026-08-24

**跟读闭环通了。** 粘一句英文 → 合成范本 → 跟读录音 → 三项分数 + 逐词单调标记。

这是这个项目第一次有完整的产品价值：在此之前 TTS 能出声、评分能打分，
但两者接不起来，用户拿不到任何反馈。

### 新增

**评分层四块**（M04）

- `parse.ts` 响应解析、`config.ts` 请求头构造、`azure.ts` provider、
  `assess.ts` 编排。四个文件覆盖率全部 100%。
- 结果分三种走向：`scored` / `unreliable` / `no_speech`——它们在界面上
  是三种完全不同的呈现，压缩成「成功或失败」会让用户为系统的正常行为
  感到困惑（decisions 0027）。
- 评分有重试，TTS 没有：TTS 失败用户再点一次就行，评分失败意味着
  那一遍朗读白读了（decisions 0028）。

**录音层四个纯函数模块**（M03）

- `pcm.ts` Float32 → Int16、`encode-wav.ts` 编码器、
  `trim-silence.ts` 掐首尾静音、`recording/state.ts` 状态机。
  171 个用例，覆盖率全部 100%。
- 状态机测了 6 状态 × 7 事件 = 42 格**完整矩阵**——状态机的穷尽性
  没法靠「我想了想觉得够了」论证。
- 写成不依赖 DOM 的纯函数，所以不引 jsdom 也不引 testing-library。

**传输层**

- `core/http/fetch-like.ts`：所有请求带显式超时，认 `Retry-After`
  的秒数与 HTTP 日期两种形态，带上 `RequestId`。
- 假 fetch 补了 `headers`、`hangs`（模拟服务端不返回响应头）、`signal`。

**界面**

- 录音、回放、逐词标记。分数只展示分档不展示原始百分数——
  实测准确度和流利度系统性偏高约 20 分（decisions 0019）。
- 完整度不展示：朗读场景专家全给满分，没有区分度。

### 修复

- **静音修剪用均方根会被单次爆音击败。** 30000 的爆音落在 160 采样
  窗口里，RMS 被拉到阈值的 23 倍——平方放大离群值。改成按占比判断。
- **请求体超限时 `destroy()` 请求**，客户端看到的是 `fetch failed`
  而不是 413。这个 bug 从 v0.1.0 就在 JSON 那条路上，从来没被测到。
- **`parseWav` 曾经只认 44 字节固定头**，ffmpeg 转出来的文件一概拒收。
- **请求体上限 64KB 只够 2 秒音频**——M01 写下时想的是 JSON。
- `vitest.live.config.ts` 缺 `hookTimeout`。

### 重构

- `errors.ts` 从 `core/tts/` 搬到 `core/`，`TtsError` → `ServiceError`。
  旧名字保留为别名，**434 个老用例一行未改就跑通**——这才证明当初的
  抽象真的是 provider 无关的。

### 被实测推翻的旧断言

- 「用 `AudioContext({sampleRate: 16000})`，重采样这一步整个消失」——
  **Firefox 上不成立**，它忽略这个约束且拒绝连接不同采样率的节点。
  Stage 0 明确不支持 Firefox（decisions 0031）。
- 「单次评分不超过 30 秒是硬上限」——**不是硬拒绝，是静默截断**。
- 「必须单声道 / 必须 16kHz」——Azure 都不强制。
- 「采样率声明不符会静默压低分数」——服务端读 WAV 头，忽略声明。

### 参考实现三处照抄不得

| 它的做法 | 问题 |
| --- | --- |
| 结尾阈值 -100 dBFS | = 0.33 个量化单位，低于 1，等于「恰好为 0」，那一半白做 |
| 切在阈值交越点 | 削掉 p/t/k 的起音前沿，准确度分数下降 |
| 直接算绝对值 | 麦克风有直流偏置时修剪完全失效且不报错 |

### 测试完整性判据从三条变五条

`decisions.md` 0026 与 0029：

4. **测试数据不能和生产数据同源**——`parseWav` 那个 bug 就是这么漏的。
5. **跨层约束要有整条链的断言**——请求体上限那个 bug 任何单层测试都抓不到。

另外把判据二收紧了：**不可达代码不写说明，直接消灭**。
不可达代码写不出测试，也就没有东西能保证它是对的。

### 真机验证

4.94 秒素材（前后各 1 秒静音）→ HTTP 200，2.0 秒返回，
掐掉首 1.10s / 尾 1.21s，剩 2.63s，准96 流100 语91.2 总95.7，
9 个词全带 `monotone`，信噪比 38.0 dB。

### 规模

| | v0.1.0 | v0.1.5 | v0.2.0 |
| --- | --- | --- | --- |
| 离线用例 | 321 | 434 | **743** |
| live 用例 | 8 | 23 | 23 |
| 测试文件 | 9 | 12 | 18 |
| 决策记录 | 14 | 26 | 34 |
| 运行时依赖 | 0 | 0 | **0** |

---

## v0.1.5 — 2026-08-23

**朝 0.2.0 走了一半。** 评分层四块做完两块，闭环还没通——所以不是 0.2.0。

### 新增

- **评分响应解析**（`providers/scoring/parse.ts`）62 个用例，覆盖率 100%。
  这是 TTS 层没有对应物的一层：TTS 的响应是字节流，验结构就够；
  评分的响应是三层嵌套 JSON，脾气很怪。
- **评分请求头构造**（`providers/scoring/config.ts`）44 个用例，覆盖率 100%。
  上限从音频上限推导而非拍数：`30 秒 × 15 字符/秒 × 2 = 900`。
- **`assertAssessable()`**：拦超时长音频。
- **效度测量工具**（`npm run validity`）：用 speechocean762 公开基准，
  拿 Azure 的分数和 5 位语言专家的共识算相关系数。
- **发音评估 live 用例** 15 个，含 7 个边缘行为。

### 修复

- **`parseWav` 只认 44 字节固定头**。RIFF 是块结构不是固定布局，
  ffmpeg 会在 `fmt ` 和 `data` 之间插 `LIST INFO`，这类合法 WAV 原来一概拒收——
  而 Azure 照单全收。321 个用例全绿，因为测试样本和生产音频出自同一个模子。
  已改为块遍历。
- **请求体上限 64KB = 2.0 秒音频**。M01 写下时想的是 JSON，
  16kHz 单声道是 32,000 字节/秒。M03 一上传录音就炸——跨里程碑的 bug。
  已拆成 `MAX_JSON_BYTES` / `MAX_AUDIO_BYTES`。
- **`vitest.live.config.ts` 缺 `hookTimeout`**，钩子仍是默认 10 秒。

### 测量结果

137 条成人样本，Azure 对人类专家的斯皮尔曼相关：

| 维度 | 相关系数 | 判读 |
| --- | --- | --- |
| 语调 | **0.908** | 强，且校准偏差最小 |
| 总分 | 0.869 | 强 |
| 流利度 | 0.808 | 强 |
| 准确度 | 0.776 | 强 |
| 完整度 | — | 专家全给满分，无区分度 |

**Azure 这条路成立。** 最想要的语调维度相关性最高，而它正是参考实现 Enjoy
拿不到的那一项。

### 被实测推翻的旧断言

- 「单次评分不超过 30 秒」性质说错了：**不是硬拒绝，是静默截断**。
  73 秒音频返回 200 + Success，完整度 49（≈35/73）。
- 「必须单声道 / 必须 16kHz」Azure 都不强制。立体声、8kHz、带 `LIST` 块的
  WAV 全部照收，分数几乎一致。
- 「采样率声明不符会静默压低分数」是错的：服务端读 WAV 头，忽略声明。

### 决定

0015–0026，共 12 条。其中 0026 给测试完整性补了两条判据：
测试数据不能和生产数据同源；跨层约束要有整条链的断言。

### 规模

| | v0.1.0 | v0.1.5 |
| --- | --- | --- |
| 离线用例 | 321 | 434 |
| live 用例 | 8 | 23 |
| 决策记录 | 14 | 26 |
| 运行时依赖 | 0 | 0 |

---

## v0.1.0 — 2026-08-23

**第一次能打开来用。** 浏览器里粘一句英文，点合成，出声。

在此之前有 2210 行代码和 321 个测试，但没有任何一个入口能打开——
0.0.0 是「有代码」，0.1.0 是「有能用的东西」，这条线值得一个版本号。

### 新增

- **本地 HTTP 服务**（`src/http/`）：三个路由，用 `node:http` 不引框架。
  请求体上限、字段校验、路径穿越防护都显式写了，没有假装框架会替你做。
- **合成页面**（`public/index.html`）：输入框、音色、语速、播放器。
  错误按分类说人话（「密钥有问题」「额度用完了」），不甩状态码。
- **文件系统音频存储**（`FileAudioStore`）：按内容哈希落盘，键做正则校验防路径穿越。
- **CI**：push 触发 typecheck 和 321 个离线用例。
- **文档**：`docs/roadmap.md` 路线图与已知缺口；README 增加「不可逾越的边界」一节。

### 修复

- **`vitest.live.config.ts` 从不加载 `.env.local`。**
  填好密钥跑 `test:live` 仍然整组跳过，而且不报错——
  「没配置就跳过」的逻辑分不清「没配」和「配了但没加载」。
  这类伪装成正常行为的 bug 最难查。
- **空字符串穿过了 `??` 兜底。**
  `.env.local` 里 `AZURE_TTS_VOICE=` 的值是 `""` 不是 `undefined`，
  `?? "en-US-AvaNeural"` 不生效，空音色名送到 Azure 换回 400。
  第一次真跑就撞上了——321 个用例一个都没覆盖到配置读取这一层。

### 决定

- **0012**：Stage 0 做本地 Web 应用，Stage 1 才上 Electron。
- **0013**：HTTP 层用 `node:http` 不引框架，运行时依赖保持为空。
- **0014**：live 测试的密钥由 vitest 配置显式注入。
- TTS 输出格式定为 **16kHz / 单声道 / 16-bit**，而不是默认的 24kHz。
  这是 Azure 发音评估唯一接受的采样率，M04 接评分时不必重采样，
  M03 的录音也能与范本音频对齐。

### 验证

| 项目 | 结果 |
| --- | --- |
| `npm test` | 321 passed，1.39s |
| `npm run test:live` | 8 passed（真实调用 Azure），18.08s |
| `npm run typecheck` | 通过 |
| 首次合成 | 1.64s |
| 同文本仅首尾空格差异 | 0.0012s，命中缓存 |
| 落盘音频 `parseWav` | 16000Hz / 1ch / 16bit |

---

## v0.0.0 — 2026-08-22

项目起点。TTS 引擎层：文本规范化、分句、SSML 构造与防注入、内容寻址缓存、
错误三线索分类、Azure provider（REST，零依赖）、WAV 结构校验、编排层。
321 个离线用例。

此版本没有可运行的入口。
