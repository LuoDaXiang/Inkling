# Live 测试配置。复制成 .env.local 再填写，.env.local 不会进仓库。
# 不配置时 npm run test:live 会自动跳过，而不是失败。

# 要测哪个 provider：kokoro | azure
INKLING_TTS_PROVIDER=

# Azure Speech（发音评估与 TTS）
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=
